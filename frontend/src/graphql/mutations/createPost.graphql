mutation createPost($title: String!, $image: String!) {
  createPost(title: $title, image: $image) {
    id
    createdAt
    title
    image
    upvoteCount
    creator {
      id
      username
      email
    }
  }
}
